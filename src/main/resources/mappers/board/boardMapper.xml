<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="board">
	<!-- 조건 없이 모든 글 출력 -->
	<select id="postList" resultType="boardDto">
		select boardingnumber, writer, title, boardingdate, viewcount
		from postboard
		order by boardingnumber desc
	</select>
	
	<!-- 작성한 글을 데이터베이스에 저장한다 -->
	<insert id="insert">
		insert into postboard (boardingnumber, title, content, writer)
		values (sequenceboard.nextval, #{ title }, #{ content }, #{ writer })
	</insert>
</mapper>